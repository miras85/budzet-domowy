# ═══════════════════════════════════════════════════════════
# DOMOWYBUDZET - Environment Variables Template
# ═══════════════════════════════════════════════════════════
#
# Instrukcja:
# 1. Skopiuj ten plik jako .env:  cp .env.example .env
# 2. Wypełnij prawdziwe wartości (nie commituj .env do git!)
#

# ───────────────────────────────────────────────────────────
# DATABASE - MySQL Connection
# ───────────────────────────────────────────────────────────
# Format: mysql+mysqlconnector://user:password@host:port/database
# Lokalne (Mac/Oracle VM): localhost
# Zdalne (jeśli MySQL osobno): IP lub hostname

DATABASE_URL=mysql+mysqlconnector://domowybudzet:your_password_here@localhost:3306/domowy_budzet

# ───────────────────────────────────────────────────────────
# SECURITY - JWT Token Secret
# ───────────────────────────────────────────────────────────
# WAŻNE: Wygeneruj silny klucz!
# Komenda: openssl rand -hex 32
# Długość: 64 znaki (32 bajty hex)

SECRET_KEY=your_64_character_secret_key_generated_by_openssl_rand_hex_32

# ───────────────────────────────────────────────────────────
# OPTIONAL - Future features
# ───────────────────────────────────────────────────────────
# Odkomentuj gdy będą potrzebne:

# SMTP (email notifications):
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASSWORD=your_app_password

# Oracle Object Storage (backup cloud):
# ORACLE_BUCKET_NAME=domowybudzet-backups
# ORACLE_NAMESPACE=your_namespace
